/*import { PrismaClient } from "@prisma/client";
import { PrismaClient as PostApprovalPrismaClient } from "../generated/postApprovalClient"; // Import the second client

// Connect to the main database
export const mainPrisma = new PrismaClient({
  datasources: { db: { url: process.env.DATABASE_URL } },
});

// Connect to the post approval database
export const postApprovalPrisma = new PostApprovalPrismaClient({
    datasources: { db: { url: process.env.POST_APPROVAL_DATABASE_URL } },
  });*/
